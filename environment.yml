# You can use this file to create a conda environment for this pipeline:
# conda env create -f environment.yml
name: dolphinnext-kb-1.0
channels:
   - conda-forge
   - bioconda
   - defaults
dependencies:
   - python=3.7
   - fastqc=0.11.8
   - multiqc=1.7
